import { kv } from "../types";

const varc: kv = {
  label: ".varc",
  javascript: "const ${1:variableName} = {{TEXT}};",
  typescript: "const ${1:variableName} = {{TEXT}};",
};

export default varc;
