import { kv } from "../types";

const _return: kv = {
  label: ".return",
  description: "return (expr);",
  javascript: "return ({{TEXT}});",
  typescript: "return ({{TEXT}});",
  javascriptreact: "return ({{TEXT}});",
  typescriptreact: "return ({{TEXT}});",
};

export default _return;
